# 前端 apps/web .env 範本
# 使用方式：cp .env.example .env 後依環境調整。

# 前端要連的後端位址（後端 Docker 的 host 映射埠，預設冷門埠 7529）。
# 注意：VITE_ 變數是 build 時烙入；於 .env / .env.local 設定，不能靠 shell 覆蓋。
VITE_SERVER_URL=http://localhost:7529
