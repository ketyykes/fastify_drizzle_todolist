# Project Skills

This directory contains project-local agent skills generated by Better Fullstack.

The `better-fullstack` skill teaches compatible agents how to use this generated
project safely: prefer the local package manager, avoid starting dev servers unless
asked, and use Better Fullstack commands for scaffold or addon changes.

Add more skills as sibling directories, each with its own `SKILL.md`.
